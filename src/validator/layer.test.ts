import { Layer } from './layer';
import { ErrorInfo } from "../validator";

import * as chai from 'chai';

const expect = chai.expect;

describe('Layer Test', () => {

    it('layer content parse' , () => {
        let errorInfo = new ErrorInfo("aaa.json", 1)

    });
});
